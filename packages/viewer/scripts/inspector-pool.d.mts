/** Private implementation fields that cannot appear in wire messages or DOM APIs. */
export const inspectorPrivateProperties: RegExp;
/** Pool repeated constants in a bundled browser script without evaluating code. */
export function inspectorPool(code: string): string;
