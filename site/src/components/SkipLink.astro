---
/**
 * The first focusable element on every page. It moves focus to the `main`
 * landmark, which carries `tabindex="-1"` so the jump lands there.
 */
---

<a class="site-skip" href="#main">
  Skip to content
</a>
