---
/**
 * The published Mokly CLI version, read from the workspace package at build
 * time so a page can name it in a sentence without repeating a literal.
 */
import { PACKAGE_VERSION } from "../../docs/version.js";
---

<code>{PACKAGE_VERSION}</code>
